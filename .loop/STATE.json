{
  "cycle": 15,
  "cp": "CP-AB",
  "phase": "TEST",
  "ticket": "CP-AB-suite-runs-once",
  "status": "CP-AB DONE, committed 82ccfbb. npm test THREE consecutive times on ONE database: 1102/0, 19/19, exit 0, byte-identical. No migration \u2014 027 still free. YOUR REPRO WAS THE RIGHT ONE. Running it twice is what a real user does and it is the thing I did not do \u2014 every CP-AA number came from a run against a database I had just dropped by hand, so I proved the harness works and never proved it works THE WAY A CONTRIBUTOR WOULD USE IT. apply-schema.mjs resets the public schema before applying. NOT via IF NOT EXISTS in migrate.sql, for the reason you gave: that lets a PARTIALLY applied schema pass silently as complete, worse than a loud failure, and migrate.sql is the production base schema. CREATE EXTENSION IF NOT EXISTS uuid-ossp is at migrate.sql:4 so the drop is safe. THE RESET IS GUARDED TWICE. Guard 1 (URL host, already there from CP-AA) exits the process with nothing between it and the DROP, so the DROP is UNREACHABLE for a non-local URL \u2014 proven by ORDER not inspection: pointing at nonexistent.invalid returns the guard's message, NOT a DNS error, which is what ordering looks like from outside. Guard 2 asks the SERVER actually reached via inet_server_addr(), because a hosts-file entry or tunnel can make 'localhost' resolve elsewhere and this is the one statement where being wrong is unrecoverable. GUARD 2 REJECTED THE LOCAL SERVER ON ITS FIRST RUN: inet_server_addr() is an inet, so ::text yields '127.0.0.1/32' and my compare failed \u2014 three runs of FATAL. Correct direction for a guard to fail in, and I am reporting rather than quietly fixing it because it is the ONLY evidence anyone has that guard 2 is live rather than decorative. host() fixes the format. NOTE IF YOU PROBE IT: guard 2 can only fire when the URL passes guard 1 but the connection lands non-local (tunnel/hosts entry) \u2014 there is no way to trigger it with a URL alone. AB1/AB2 PASS. AB3 PASS (three refusal shapes, all exit 2). AB4 PASS \u2014 identical totals AND I checked the DB directly: contacts/tenants row counts identical after run 3 and run 4, so no state accumulates. AB5 PASS \u2014 injected failure gives 1102 passed / 1 failed, names unit-tenants, exit 1, then green again next run on the same DB with no manual dropdb. F47 FILED: the reset makes the scratch DB single-tenant to one run \u2014 two concurrent `npm test` against the SAME DATABASE_URL_TEST would now have one drop the other's schema mid-run, where previously they would merely have collided on the port guard. Worth a README note or an advisory lock. STILL OUTSTANDING: the golden journey has STILL not been re-run by me.",
  "handoff": "orch",
  "updated_by": "build",
  "build_ticket": "CP-AB repeatable suite runs (BUILT 82ccfbb)",
  "orch_ticket": "verify each as it lands; goal audit at .loop/GOAL_CONFORMANCE_AUDIT.md",
  "next_ticket": "F38 \u2014 the anchored scheduler (a PORT of nurturing-engine dispatcher.py:160-204)",
  "backup": {
    "branch": "origin/feat/consolidation",
    "tip": "663eb1f",
    "code_and_record_backed_up": true,
    "main_untouched": "9ff21cb"
  }
}